-- Staging: clean raw orders, exclude cancelled, ensure valid amounts
from source.raw.orders
where status != "cancelled"
where amount > 0
derive {
    order_amount: amount,
    order_year: order_date
}
select {
    order_id,
    customer_id,
    order_date,
    order_year,
    status,
    order_amount
}
