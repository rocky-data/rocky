# Rocky

> Rust SQL transformation engine. Replaces dbt's core responsibilities (DAG resolution, incremental logic, SQL generation, schema management) with a compiled, type-safe approach. No Jinja. No manifest. No parse step.

## Overview

Rocky is a CLI tool written in Rust that replicates data from sources (Fivetran) into warehouses (Databricks) using config-driven pipelines. It also supports custom SQL transformation models with TOML configuration.

Key concepts:
- Bronze layer: Config-driven replication from Fivetran to Databricks (no SQL files needed)
- Silver layer: Custom SQL models with TOML configuration (sidecar .sql + .toml files)
- Schema patterns: Parse source schema names into structured components for target mapping
- Inline checks: Data quality checks (row count, column match, freshness) run during replication
- Embedded state: Watermarks stored in local redb database

## Commands

- `rocky init [path]` — Scaffold a new project
- `rocky validate` — Check config without connecting to APIs
- `rocky discover` — List available connectors and tables
- `rocky plan --filter key=value` — Generate SQL without executing
- `rocky run --filter key=value` — Execute the full pipeline
- `rocky state` — Show stored watermarks

## Configuration

Rocky reads `rocky.toml` with sections: [source], [warehouse], [target], [replication], [checks], [governance]

## Dagster Integration

The `dagster-rocky` Python package provides:
- `RockyResource` — Dagster resource wrapping the CLI
- `load_rocky_assets()` — Auto-discover assets from Rocky sources
- `emit_check_results()` / `emit_materializations()` — Convert results to Dagster events
- `RockyComponent` — State-backed component for scalable discovery
- Pydantic models for all JSON output types

## Docs

- [Introduction](getting-started/introduction/)
- [Installation](getting-started/installation/)
- [Quickstart](getting-started/quickstart/)
- [Bronze Layer](concepts/bronze-layer/)
- [Silver Layer (Models)](concepts/silver-layer/)
- [Schema Patterns](concepts/schema-patterns/)
- [CLI Reference](reference/cli/)
- [Configuration](reference/configuration/)
- [JSON Output](reference/json-output/)
- [Dagster Integration](dagster/introduction/)
