# rocky-vscode — Agent Instructions

VS Code extension for the Rocky SQL transformation engine. LSP client that communicates with `rocky lsp` over stdio, plus commands for AI features, diagnostics, and operational tasks.

## Architecture

```
Extension (TypeScript)  ──stdio JSON-RPC──▶  rocky lsp (Rust binary)
```

The extension is an LSP client. All language intelligence (diagnostics, completion, hover, go-to-definition, rename, etc.) comes from the Rocky CLI's built-in LSP server. The extension handles:
- LSP client lifecycle (start/stop/restart) — `lspClient.ts`
- Status bar with server state + error count — `extensionState.ts`
- 25 commands grouped by concern under `src/commands/`
- Tree views (models, sources, runs) — `src/views/`
- Webviews (run summary, doctor, lineage) — `src/webviews/`
- Code lens, task provider, test explorer — top-level `src/` files
- Type-safe CLI output via generated TypeScript interfaces — `src/types/generated/`

## Project Structure

```
src/
├── extension.ts          # activate/deactivate — wires LSP client + command registry
├── lspClient.ts          # LSP client lifecycle (start/stop/restart)
├── rockyCli.ts           # Subprocess helpers (execFile + progress wrappers)
├── extensionState.ts     # Extension-wide state management
├── config.ts             # Workspace + settings access
├── output.ts             # Shared output channel
├── codeLens.ts           # Code lens provider
├── taskProvider.ts       # VS Code task provider
├── testExplorer.ts       # Test explorer integration
├── types/
│   ├── rockyJson.ts      # Compat shim re-exporting from generated/ + hand-written interfaces
│   └── generated/        # Autogenerated from ../../../../schemas/*.schema.json (do not edit)
├── commands/             # One file per concern; index.ts registers all 25 commands
│   ├── index.ts          # Single registration point
│   ├── ai.ts             # aiGenerate, aiSync, aiExplain, aiTest
│   ├── compile.ts        # compile, validate, ci
│   ├── hooks.ts          # hooksList, hooksTest
│   ├── inspect.ts        # history, metrics
│   ├── lineage.ts        # showLineage (DOT → SVG webview)
│   ├── migration.ts      # importDbt, validateMigration
│   ├── ops.ts            # doctor, optimize
│   ├── run.ts            # run, plan, discover, compare
│   ├── storage.ts        # compact, archive, profileStorage
│   ├── test.ts           # test
│   └── ui.ts             # Shared editor/prompt/confirm helpers
├── views/
│   ├── modelsView.ts     # Models tree view
│   ├── sourcesView.ts    # Sources tree view
│   └── runsView.ts       # Runs tree view
└── webviews/
    ├── runSummary.ts     # Run summary webview
    ├── doctor.ts         # Doctor results webview
    └── htmlUtil.ts       # HTML escaping utilities
syntaxes/
└── rocky.tmLanguage.json # TextMate grammar for .rocky files
snippets/
└── rocky.json            # 12 code snippets
schemas/
├── rocky-config.schema.json   # JSON Schema for *.rocky.toml validation
└── rocky-project.schema.json  # JSON Schema for rocky project config
themes/
└── rocky-semantic.json   # Semantic token colors
icons/
└── rocky-icon.svg        # File icon
```

## Coding Standards

- Target: ES2022, Module: CommonJS, strict mode
- VS Code API minimum: 1.85.0
- Use `cp.execFile()` for subprocess calls (not `cp.exec()` — no shell injection)
- Use `vscode.window.withProgress()` for long-running commands
- Escape HTML in webview content via `escapeHtml()`

## Common Commands

```bash
npm install               # Install dependencies
npm run compile           # TypeScript → JS (out/)
npm run watch             # Continuous compile
npm run test:unit         # Vitest unit tests (fast)
npm test                  # Full Electron integration tests
npm run lint              # ESLint
npm run package           # Build .vsix
```

## Extension Commands (25)

| Command | What It Does |
|---------|-------------|
| `rocky.restartServer` | Restart LSP server |
| `rocky.showLineage` | Render model DAG as interactive SVG |
| `rocky.compile` | Type-check models |
| `rocky.validate` | Validate config and models |
| `rocky.ci` | Combined compile + test |
| `rocky.test` | Run local tests via DuckDB |
| `rocky.run` | Execute the full pipeline |
| `rocky.plan` | Preview SQL (dry-run) |
| `rocky.discover` | List connectors and tables |
| `rocky.compare` | Shadow vs production comparison |
| `rocky.history` | Run history with trend analysis |
| `rocky.metrics` | Quality metrics with alerts |
| `rocky.compact` | Generate OPTIMIZE/VACUUM SQL |
| `rocky.archive` | Partition archival |
| `rocky.profileStorage` | Column encoding recommendations |
| `rocky.aiGenerate` | Create model from natural language |
| `rocky.aiSync` | Detect + propose schema changes |
| `rocky.aiExplain` | Generate intent docs from model |
| `rocky.aiTest` | Generate test cases |
| `rocky.importDbt` | Convert dbt project to Rocky |
| `rocky.validateMigration` | Validate dbt → Rocky migration |
| `rocky.hooksList` | List configured hooks |
| `rocky.hooksTest` | Fire hook event for testing |
| `rocky.doctor` | Health checks |
| `rocky.optimize` | Cost & strategy analysis |

## Related Projects

- [rocky](https://github.com/rocky-data/rocky) — Core CLI + engine (Rust)
- [dagster-rocky](https://github.com/rocky-data/rocky/tree/main/integrations/dagster) — Dagster integration (Python)
