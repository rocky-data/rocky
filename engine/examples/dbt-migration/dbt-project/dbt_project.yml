name: ecommerce
version: '1.0.0'
config-version: 2

profile: 'ecommerce'

model-paths: ["models"]

models:
  ecommerce:
    staging:
      +materialized: view
      +schema: staging
    marts:
      +materialized: table
      +schema: analytics
